// @ts-ignore -- no intellisense for js-datepicker.
const mainDate = datepicker("#date");
mainDate.setMin(new Date()); // set today's date
// @ts-ignore -- no intellisense for js-datepicker.
const dueDate = datepicker("#due-date");
dueDate.setMin(new Date());